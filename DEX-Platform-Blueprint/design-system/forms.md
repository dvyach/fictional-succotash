# Forms

Inputs use `--adx-radius-sm`, border-default, focus ring brand.  
Validation inline; error text associated via `aria-describedby`.  
Patterns: case create, incident update, automation approve, remote action, save view.
