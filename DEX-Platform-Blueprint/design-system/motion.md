# Motion

| Token | Value | Use |
|-------|-------|-----|
| `--adx-duration-fast` | 120ms | Hover |
| `--adx-duration-base` | 200ms | Panels |
| `--adx-duration-slow` | 320ms | Drawers |
| easings | standard / emphasized / exit | tokens.md |

Honor `prefers-reduced-motion: reduce`. Chart entrance once per mount.
