# Charts

Defaults: gridline subtle, caption axes, tooltip elevation-2, threshold bands at 20% opacity.  
Types: area, line, bar, stacked, donut, heatmap, scatter, funnel, sankey, combo, sparkline, gauge, bullet, waterfall, box, calendar.  
Empty/error/loading standardized. Compose via Volume 2E primitives (`w-chart-*`).
