# Frontend Engineering Guide

- React + TypeScript; `Adx*` components; URL state for scope/time  
- Bind KPIs by Volume 3A IDs only  
- Compose widgets from Volume 2E primitives + domain `w-*`  
- a11y and visual regression in CI  
- Code-split by domain route  

See Volumes 3C–3E and `design-system/`.
